# Railway / Nixpacks: default Rust is often 1.85.x, which cannot build current deps
# (`time` 0.3.47, `home` 0.5.12, etc. require rustc >= 1.88 per Cargo.toml).
[variables]
NIXPACKS_RUST_VERSION = "1.88.0"
